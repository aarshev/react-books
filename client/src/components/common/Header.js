export const Header = () => {
    return(
        <header className="header">
            <div className="logo">
                <span className="course">Book Library</span>
            </div>
        </header>
    )
}